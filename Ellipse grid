void setup() {
  size(400,400);
  background(0);
  fill(255);
  noStroke();
}
void draw () {
int y = 20;
while (y < 400) {
  int x = 20;
  while (x < 400) {
    ellipse(x,y,14,14);
    x = x + 20;
   }
   y = y + 20;
}
}
